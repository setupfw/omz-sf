# Enhance
sudo history command-not-found zbell

# `cd` Manager
z jump dirhistory

# Alias
aliases alias-finder globalias

# other programs:
extract git gitignore shell-proxy kate node

# completions:
fd lxd adb minikube
